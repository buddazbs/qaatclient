# Admin Panel 🚀

Современная админ-панель для IT стартапа с адаптивной версткой, анимациями и удобным интерфейсом.

## О проекте

Это не просто панель управления — это инструмент, который делает работу с автотестами и статистикой лёгкой и приятной. Построена на **React**, **TypeScript** и **Ant Design**, с архитектурой **Feature-Sliced Design (FSD)**, чтобы код оставался чистым и масштабируемым.

Приложение адаптировано под все устройства, с плавными анимациями и быстрым откликом интерфейса.

---

## 🔥 Основные фишки

* **Красивый и современный UI**: Используем Ant Design, чтобы интерфейс был удобным и понятным
* **Адаптивность**: Панель выглядит отлично на любом экране
* **Плавные анимации**: Интерфейс «живёт» и радует глаз
* **JWT-аутентификация**: Безопасность на уровне профессионалов
* **Два типа пользователей**: Админ и обычный юзер с разными правами
* **Темная/светлая тема**: Переключение без потери данных
* **FSD-архитектура**: Чистая структура проекта, легко добавлять новые фичи

---

## 🛠 Функциональность

### Администратор:

* Дашборд с общей статистикой
* Управление автотестами (запуск/остановка)
* Просмотр и перезапуск результатов тестов
* Отправка коллбэков
* Настройки аккаунта

### Обычный пользователь:

* Отправка коллбэков
* Настройки аккаунта

---

## ⚡ Технологии

* React 18
* TypeScript
* Ant Design
* Redux Toolkit
* React Router v6
* Feature-Sliced Design (FSD)

---

## 🚀 Установка и запуск

1. Склонируйте репозиторий:

```bash
git clone git@github.com:buddazbs/qaatclient.git
cd qaatclient
```

2. Установите зависимости:

```bash
npm install
```

3. Запустите приложение в режиме разработки:

```bash
npm start
```

4. Откройте [http://localhost:3000](http://localhost:3000) и тестируйте панель

---

## 🌐 Деплой на GitHub Pages

1. Установите пакет `gh-pages`:

```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:

```json
"homepage": "https://<ваш_пользователь>.github.io/<ваш_репозиторий>",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
}
```

3. Выполните деплой:

```bash
npm run deploy
```

4. После этого приложение будет доступно по ссылке из `homepage`.

---

## 🧑‍💻 Демо-аккаунты

* **Админ**: `admin` / `password`
* **Пользователь**: `user` / `password`

---

## 📂 Структура проекта (FSD)

```
src/
├── app/        # Инициализация приложения и провайдеры
├── pages/      # Страницы приложения
├── widgets/    # Самостоятельные блоки функциональности
├── features/   # Бизнес-функции
├── entities/   # Бизнес-сущности
├── shared/     # Переиспользуемые модули и утилиты
└── index.tsx   # Точка входа приложения
```
